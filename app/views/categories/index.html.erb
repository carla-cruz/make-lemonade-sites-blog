<%= form_for [@category] do |f| %>
 <%= f.label :name %><br>
 <%= f.text_field :name %>
 
 <%= f.submit "Add Category" %>
 <% end %>

<% @categories.each do |cat| %>
<%= cat.name %>
<%= link_to "Delete", category_path(cat), method: :delete %><br>

<% end %>
